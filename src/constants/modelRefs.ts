

const userRefs = ['Admin', 'SubAdmin', 'Organizer', 'staff', 'user'];

const formatNames = ['knockout', 'double_elimination_bracket', 'round_robbin'];

const formatRefs = ['knockout', 'doubleKnockout', 'RoundRobbin'];

const gameTypeRefs = ['team', 'individual'];

const participantRefs = ['team', 'player'];

const fixingTypeEnums = ['sequential', 'random', 'manual', 'top_vs_bottom'];

const scoreTypeEnums = ['Top Score', 'Best Of', 'Race To', 'Cricket', 'Football'];


export {
    userRefs,
    formatNames,
    formatRefs,
    gameTypeRefs,
    participantRefs,
    fixingTypeEnums,
    scoreTypeEnums
}